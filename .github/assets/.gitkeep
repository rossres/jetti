# Drop visual assets here.
#
# Expected files (designer brief: docs/jetti-landing-and-github-designer-brief-2026-05-07.md):
#   hero-light.png        — main banner, GitHub light theme
#   hero-dark.png         — main banner, GitHub dark theme
#   demo-install.gif      — `install jetti` CLI flow, ~5–8s loop
#   demo-reviewer.gif     — reviewer editing a live site, ~5–8s loop
#   demo-apply.gif        — `npx jetti apply` running, ~5–8s loop
#
# Already landed:
#   social-preview.png    — 1280×640 brand-kit-aligned card (cream / copper / honey,
#                           jetti wordmark, install command, MIT · v0.4 · skill footer).
#                           Temporary; replace when the designer ships a polished version.
#                           Upload to repo via Settings → Social preview.
